# Keeps the pinned action SHAs from going stale. Dependabot opens a pull request
# when a new release lands, updating the SHA and the version comment together.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: ci
    labels:
      - dependencies
